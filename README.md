# C2Shield
C2Shield is a project for detecting Remote Access Tools (RATs) through system-level behavioral indicators. It combines a controlled RAT with a host-based detection system that monitors processes, persistence, and network activity, focusing on behavior instead of signatures.
